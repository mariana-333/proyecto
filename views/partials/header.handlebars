<header>
    <nav class="navbar top-nav">
      <div class="nav-logo">
        <img src="/img/logo.webp" alt="Chass.com Logo">
        <a href="/">Chass.com</a>
      </div>
      <ul class="nav-links">
        <li><a href="/play"><i class="fas fa-chess"></i> Jugar</a></li>
        {{#if isLoggedIn}}
        <li><a href="/my-games"><i class="fas fa-gamepad"></i> Mis Partidas</a></li>
        {{/if}}
        <li><a href="/profile"><i class="fas fa-user"></i> Perfil</a></li>
      </ul>
      {{#if isLoggedIn}}
        <div class="user-welcome">
          <span>Bienvenido, {{currentUser.username}} !</span>
          <a href="/logout" class="logout-btn"><i class="fas fa-sign-out-alt"></i> Cerrar sesión</a>
        </div>
      {{else}}
        <div class="auth-buttons">
          <a href="/login" class="login-btn"><i class="fas fa-sign-in-alt"></i> Iniciar sesión</a>
          <a href="/register" class="register-btn"><i class="fas fa-user-plus"></i> Registrarse</a>
        </div>
      {{/if}}
    </nav>
    <nav class="navbar bottom-nav">
      <ul class="category-links">
        <li><a href="/rulesandhistory"><i class="fas fa-book"></i> Reglas e Historia</a></li>
        <li><a href="/developers"><i class="fas fa-code"></i> Desarrolladores</a></li>
      </ul>
    </nav>
  </header>